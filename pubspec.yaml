name: mastodon_flutter
description: A new Flutter package project.
version: 0.1.0+3
homepage: https://github.com/lukepighetti/mastodon_flutter

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  mastodon_dart: ^0.1.0+9
    # git: https://github.com/lukepighetti/mastodon_dart
    # path: ../mastodon_dart

  html: ^0.14.0+3
  shared_preferences: ^0.5.6+3
  url_launcher: ^5.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
